import java.util.Scanner;

public class MyMain {

    /**
     * Checks whether a word exists as a complete word in a given string (i.e. it isn't part of a larger word).
     * <p>
     * For example, <code>existsAsWord("hello", "hello world")</code> and <code>existsAsWord("cats", "I like cats.")
     * </code> should return true but <code>existsAsWord("taco", "tacocat")</code> and
     * <code>existsAsWord("cat", "tacocat")</code> should return false.
     * @param word The word to check for in the string. Case-sensitive. Guaranteed not to be empty or null, and to
     *             contain only letters.
     * @param str The string in which to check for the word. Case-sensitive. Guaranteed not to be null.
     * @return Whether the word exists as a complete word in the string.
     */
    public static boolean existsAsWord (String word, String str) {
        return false;
    }
    
    public static void main(String[] args) {
    }
}
